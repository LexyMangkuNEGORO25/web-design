include pug/component/_component.pug 

doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        block navigasi
            -data-link = ['home','blog','about','contact'] 
            +navigasi(data_link)
        block header 
        block content 
        block footer 
            +footer()